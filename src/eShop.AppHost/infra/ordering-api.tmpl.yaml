api-version: 2024-02-02-preview
location: {{ .Env.AZURE_LOCATION }}
identity:
  type: UserAssigned
  userAssignedIdentities:
    ? "{{ .Env.AZURE_CONTAINER_REGISTRY_MANAGED_IDENTITY_ID }}"
    : {}
properties:
  environmentId: {{ .Env.AZURE_CONTAINER_APPS_ENVIRONMENT_ID }}
  configuration:
    activeRevisionsMode: single
    runtime:
      dotnet:
        autoConfigureDataProtection: true
    ingress:
      external: false
      targetPort: {{ targetPortOrDefault 8080 }}
      transport: http
      allowInsecure: true
    registries:
      - server: {{ .Env.AZURE_CONTAINER_REGISTRY_ENDPOINT }}
        identity: {{ .Env.AZURE_CONTAINER_REGISTRY_MANAGED_IDENTITY_ID }}
    secrets:
      - name: connectionstrings--eventbus
        value: amqp://guest:{{ uriEncode (securedParameter "eventbus_password" )}}@eventbus:5672
      - name: connectionstrings--orderingdb
        value: Host=postgres;Port=5432;Username=postgres;Password={{ securedParameter "postgres_password" }};Database=orderingdb
      - name: eventbus-password
        value: '{{ securedParameter "eventbus_password" }}'
      - name: orderingdb-password
        value: '{{ securedParameter "postgres_password" }}'
  template:
    containers:
      - image: {{ .Image }}
        name: ordering-api
        env:
          - name: AZURE_CLIENT_ID
            value: {{ .Env.MANAGED_IDENTITY_CLIENT_ID }}
          - name: ASPNETCORE_FORWARDEDHEADERS_ENABLED
            value: "true"
          - name: EVENTBUS_HOST
            value: eventbus
          - name: EVENTBUS_PORT
            value: "5672"
          - name: EVENTBUS_URI
            value: amqp://guest:{{ uriEncode (securedParameter "eventbus_password" )}}@eventbus:5672
          - name: EVENTBUS_USERNAME
            value: guest
          - name: HTTP_PORTS
            value: '{{ targetPortOrDefault 0 }}'
          - name: Identity__Url
            value: https://identity-api.{{ .Env.AZURE_CONTAINER_APPS_ENVIRONMENT_DEFAULT_DOMAIN }}
          - name: ORDERINGDB_DATABASE
            value: orderingdb
          - name: ORDERINGDB_HOST
            value: postgres
          - name: ORDERINGDB_JDBCCONNECTIONSTRING
            value: jdbc:postgresql://postgres:5432/orderingdb
          - name: ORDERINGDB_PORT
            value: "5432"
          - name: ORDERINGDB_URI
            value: postgresql://postgres:{{ uriEncode (securedParameter "postgres_password" )}}@postgres:5432/orderingdb
          - name: ORDERINGDB_USERNAME
            value: postgres
          - name: OTEL_DOTNET_EXPERIMENTAL_OTLP_EMIT_EVENT_LOG_ATTRIBUTES
            value: "true"
          - name: OTEL_DOTNET_EXPERIMENTAL_OTLP_EMIT_EXCEPTION_LOG_ATTRIBUTES
            value: "true"
          - name: OTEL_DOTNET_EXPERIMENTAL_OTLP_RETRY
            value: in_memory
          - name: ConnectionStrings__eventbus
            secretRef: connectionstrings--eventbus
          - name: ConnectionStrings__orderingdb
            secretRef: connectionstrings--orderingdb
          - name: EVENTBUS_PASSWORD
            secretRef: eventbus-password
          - name: ORDERINGDB_PASSWORD
            secretRef: orderingdb-password
    scale:
      minReplicas: 1
tags:
  azd-service-name: ordering-api
  aspire-resource-name: ordering-api
